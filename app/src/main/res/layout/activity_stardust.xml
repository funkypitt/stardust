<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Game screen fills everything -->
    <ui.EmuView
        android:id="@+id/emu_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Fire button: left edge, vertically centered -->
    <Button
        android:id="@+id/btn_feu"
        android:layout_width="56dp"
        android:layout_height="72dp"
        android:layout_gravity="left|center_vertical"
        android:layout_marginLeft="4dp"
        android:text="\uD83D\uDD25"
        android:textSize="24sp"
        android:background="@drawable/btn_c64_red"
        android:hapticFeedbackEnabled="true" />

    <!-- Right-side swipe zone: 3 stacked action bands, no gaps -->
    <ui.ActionStripView
        android:id="@+id/action_strip"
        android:layout_width="100dp"
        android:layout_height="match_parent"
        android:layout_gravity="right" />

</FrameLayout>
